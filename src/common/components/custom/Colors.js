import { getProjectProps } from '../../../utils/ConfigUtils';
import { PROJECT_CONFIG_PROPS } from '../../constants';

const Colors = getProjectProps(PROJECT_CONFIG_PROPS.THEME);

export default Colors;
