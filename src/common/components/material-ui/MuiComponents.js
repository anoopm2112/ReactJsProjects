import {
    FormControl, FormHelperText, InputLabel, Container,
    Grid, Tabs, Tab, Box, CssBaseline, Drawer, Typography,
    Toolbar, Divider, Avatar, IconButton, Badge, List, ListItem,
    ListItemText, Collapse, Menu, MenuItem, ListItemIcon, Link,
    Select, Table, TableBody, TableCell, TableContainer, Paper,
    TablePagination,
    TableHead, TableRow, Card, CardContent, AppBar, CardHeader, TextField, Button, Stepper, Step, StepLabel,
    FormGroup, Modal, Chip, Dialog, ListSubheader, Checkbox, Icon
} from '@material-ui/core';

const MaterialComponents = {
    FormControl, FormHelperText, InputLabel, Container,
    Grid, Tabs, Tab, Box, CssBaseline, Drawer, Typography,
    Toolbar, Divider, Avatar, IconButton, Badge, List, ListItem,
    ListItemText, Collapse, Menu, MenuItem, ListItemIcon, Link,
    Select, Table, TableBody, TableCell, TableContainer, Paper, TablePagination,
    TableHead, TableRow, Card, CardContent, AppBar, CardHeader, TextField, Button, Stepper, Step, StepLabel,
    FormGroup, Modal, Chip, Dialog, ListSubheader, Checkbox, Icon
};

export default MaterialComponents;
