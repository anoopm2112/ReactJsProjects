/**
 *  PROJECT : value should be configs -> project -> [folder]
 */
const PROJECT = {
    CFW: 'CFW'
};

const MODE = {
    LOCAL: 'local',
    DEVELOPMENT: 'dev',
    STAGING: 'stage',
    TEST: 'test',
    PRODUCTION: 'prod'
};

// eslint-disable-next-line no-undef
module.exports = { PROJECT, MODE };
