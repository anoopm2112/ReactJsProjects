import { BlackAndWhite as Theme } from '../../../assets/theme';

const themes = {
    local: {
        ...Theme
    },
    dev: {
        ...Theme
    },
    stage: {
        ...Theme
    },
    test: {
        ...Theme
    },
    prod: {
        ...Theme
    }
};

export default themes;
